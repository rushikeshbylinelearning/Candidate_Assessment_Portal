const router = require('express').Router();

// Workflow routes - placeholder (module not yet implemented)
router.get('/', (req, res) => res.json({ message: 'Workflow module not yet implemented' }));

module.exports = router;
